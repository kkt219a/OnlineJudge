package ps.programmers.level2;

// 2018 KAKAO BLIND RECRUITMENT - [3차] 파일명 정렬
// 2차 - 2022/04/02 17:52 ~ 18:02 중단
public class FileNameSorting {
	// file을 숫자가 최초로 나오기 전과 후로 나누기(Head, Head 이후로 분리)
	// Head 이후를 앞에서부터 0부터 4까지 문자열을 만든다. 4 이전에 숫자가 끝나면 거기서 커트한다.
	// 이후 부분을 tail로 둔다.
	// 객체를 head 사전순으로 정렳하는데, 대소문자 구분을 두지 않는다.
	// 객체를 number(String)로 정렬하는데, Integer로 바꿔서 정렬한다.
	// 위 두개가 같다면 처음 있던 순서대로 출력해준다.
	public String[] solution(String[] files) {
		String[] answer = {};
		return answer;
	}
}
